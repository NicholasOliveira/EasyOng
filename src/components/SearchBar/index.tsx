import React from 'react';
import { View, Text } from 'react-native';

import { Container, TextView } from './styles';

const SearchBar: React.FC = () => {
  return <Container>
          <TextView>Search Bar</TextView>
         </Container>;
}

export default SearchBar;